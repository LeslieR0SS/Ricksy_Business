package edu.poniperro.ricksy.business;

import org.junit.Test;

import java.util.ArrayList;

import static org.junit.Assert.assertEquals;

public class UfosParkTest {

    /*@Test
    public void addTest(){
        UfosPark flota = new UfosPark();
        String[] ufosID = { "unx", "dox",};
        for (String ovni : ufosID) {
            flota.add(ovni);
        }

        assertEquals("unx", flota.getOvni(0));
        assertEquals("dox", flota.getOvni(1));
        //assertEquals(true, true);
        System.out.println("Los ovnis actuales son: " );
        for(int i=0; i< flota.getNumeroOvnis(); i++)
            System.out.println(flota.getOvni(i));
    }*/

/*
    @Test
    public void addTest2(){
        UfosPark flota = new UfosPark();
        String[] ufosID = { "unx", "dox", "trex" };
        for (String ovni : ufosID) {
            flota.add(ovni);
        }

        assertEquals("unx", flota.getOvni(0));
        assertEquals("dox", flota.getOvni(1));
        assertEquals("trex", flota.getOvni(2));
        //assertEquals(true, true);
        System.out.println("Los ovnis actuales son: " );
        for(int i=0; i< flota.getNumeroOvnis(); i++)
            System.out.println(flota.getOvni(i));
    }
*/
}

